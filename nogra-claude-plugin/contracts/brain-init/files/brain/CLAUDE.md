# Brain

This is the workspace's opt-in deep-work knowledge vault: `raw/` holds
immutable sources, `wiki/` holds compiled pages, `index.md` is the entry
point.

Pull-first: read `index.md` first, and only when the work actually needs
deep background. Never auto-load this folder at session start. It is
downstream of `.nogra/` and other live operational state, not a separate
authority.
