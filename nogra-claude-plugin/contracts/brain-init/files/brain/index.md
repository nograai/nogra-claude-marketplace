# {{workspaceName}} Brain Index

Entry point for this workspace's knowledge vault. `raw/` holds immutable
sources; `wiki/` holds compiled pages. Add an entry here as each page is
written.

- (no pages yet)
