# Reviewer Task Tracker Workspace

Use this folder for the build and checkpoint reviewer examples.

Prompt 1:

```text
Build me a small local task tracker in this workspace.
```

Prompt 2:

```text
Save a checkpoint of what we did, what changed, and what remains.
```

Nogra should turn the build request into a brief before implementation, wait for
GO, dispatch the approved work, verify the result against local evidence, and
then record a local checkpoint when asked.
